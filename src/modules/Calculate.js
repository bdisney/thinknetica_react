class Calculate {
  static sum(num_1, num_2) { return num_1 + num_2 };
  static subtract(num_1, num_2) { return num_1 - num_2 };
  static multiply(num_1, num_2) { return num_1 * num_2 };
  static divide(num_1, num_2) { return num_1 / num_2 };
}

export default Calculate;
